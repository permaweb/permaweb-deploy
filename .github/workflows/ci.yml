name: CI

on:
  pull_request:
    branches:
      - main
      - alpha

jobs:
  ci:
    name: ${{ matrix.task }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        task: [lint, format, build, test]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ${{ matrix.task }}
        run: |
          case "${{ matrix.task }}" in
            lint)
              pnpm lint
              ;;
            format)
              pnpm format:check
              ;;
            build)
              pnpm build
              ;;
            test)
              pnpm test:run
              ;;
          esac

  notify:
    name: Slack Notification
    runs-on: ubuntu-latest
    needs: ci
    if: always()
    steps:
      - name: Send Slack Notification
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_CHANNEL: permaweb-deploy
          SLACK_COLOR: ${{ job.status == 'success' && 'good' || 'danger' }}
          SLACK_ICON_EMOJI: ${{ job.status == 'success' && ':white_check_mark:' || ':x:' }}
          SLACK_TITLE: CI ${{ job.status == 'success' && 'Succeeded' || 'Failed' }}
          SLACK_MESSAGE: |
            PR #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}
            Author: ${{ github.event.pull_request.user.login }}
            Lint: ${{ needs.ci.outputs.lint }}
            Format: ${{ needs.ci.outputs.format }}
            Build: ${{ needs.ci.outputs.build }}
            Test: ${{ needs.ci.outputs.test }}
          SLACK_FOOTER: "permaweb-deploy"
          SLACK_USERNAME: GitHub Actions
